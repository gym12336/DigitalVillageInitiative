# 后端环境变量示例。复制为 .env 后填入真实值（.env 不入仓）。
# JWT 签名密钥——生产务必换成足够长的随机串。
JWT_SECRET=dev-only-change-me-in-production
# 后端监听端口（Vite proxy 默认转发到此）。
PORT=3001
# DeepSeek API 密钥（OpenAI 兼容）。留空则方案生成走规则版兜底。
DEEPSEEK_API_KEY=
# Bing Search API 密钥（Azure 门户申请）。留空则联网搜索静默跳过。
BING_SEARCH_API_KEY=
